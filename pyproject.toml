[project]
name = "minimal-mcp-ping-server"
version = "0.1.0"
description = "A minimal MCP server with a single ping tool."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.25,<2",
]
